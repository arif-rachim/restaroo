

export function useDeliveryChargeCalculator(){
    //const {store} = useAppContext();
    //const deliveryAddress = useStoreValue(store,s => s.addresses.find(a => a.defaultAddress),[]);
    //console.log('Here we need to calculate delivery address with the source of the restaurant',deliveryAddress);
    return 6;
}
